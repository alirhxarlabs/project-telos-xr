<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Network permissions (from Android version) -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.CHANGE_WIFI_MULTICAST_STATE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.SCENE_UNDERSTANDING" />

    <!-- AndroidXR features - spatial required for stereo video -->
    <uses-feature android:name="android.hardware.xr.headtracking" android:required="true" />
    <uses-feature android:name="android.hardware.xr.spatial" android:required="true" />

    <!-- Allow lower minSDK for non-XR devices -->
    <uses-sdk tools:overrideLibrary="androidx.xr.scenecore, androidx.xr.compose"/>

    <application
        android:allowBackup="true"
        android:enableOnBackInvokedCallback="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.TelosXR"
        tools:targetApi="31">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:theme="@style/Theme.TelosXR">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <!-- AndroidXR metadata -->
            <meta-data
                android:name="com.google.android.xr.activity"
                android:value="true" />

            <!-- Request spatial environment by default -->
            <meta-data
                android:name="com.google.android.xr.spatial.home_space_enabled"
                android:value="true" />
        </activity>

        <!-- FEC Receiver Service (from Android version) -->
        <service
            android:name=".network.FECReceiverService"
            android:enabled="true"
            android:exported="false"
            android:foregroundServiceType="dataSync" />
    </application>
</manifest>
